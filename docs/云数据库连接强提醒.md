# 云数据库连接强提醒（项目级约束）

## 唯一正确连接（必须统一）

`mongodb://root:6scldk9f@dbconn.sealosbja.site:39056/?directConnection=true`

## 强制规则

1. 每次读取、修改、导入、统计、排查数据库数据前，先核对 `MONGODB_URI` 是否为上方连接。
2. 严禁使用历史旧连接（例如 `30289` 端口）。
3. 新增脚本、文档、说明时，凡涉及数据库连接，必须只写本文件中的连接。
4. 如果发现任何文件中出现非 `39056` 端口，立即停止操作并先修正。

## 执行前核对清单（每次都做）

1. 检查 `web/.env.local` 的 `MONGODB_URI`。
2. 检查系统环境变量是否覆盖了 `MONGODB_URI`。
3. 运行数据库脚本前，再次确认目标连接字符串完整一致。

## 适用范围

- 本仓库全部业务数据读写
- 导入/导出脚本
- 本地调试与线上排查

---

最后更新：2026-03-02
